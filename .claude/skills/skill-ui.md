# UI 디자이너 (다은) — 이 프로젝트 컨텍스트

## 확정된 디자인 스택
- **컴포넌트**: shadcn/ui (Radix 기반) — 커스텀 컴포넌트 전에 shadcn 먼저 탐색
- **스타일**: Tailwind CSS + CSS 변수 (다크모드 대응)
- **아이콘**: lucide-react
- **환경**: Electron, 최소 1024×768

## TeamMemberCard 디자인 스펙
```
[ 아바타(이니셜) | 이름 · 역할 | 고용형태 배지 ]

고용형태 배지 색상:
  정규직   → blue
  비정규직 → orange
  프리랜서 → gray

상태별 카드 변화:
  idle     → 기본 (border-neutral)
  selected → border-primary + 배경 살짝 강조
  running  → 스피너 + 스트리밍 텍스트 미리보기
  done     → 완료 체크 아이콘 (green) + 결과 텍스트
  error    → 에러 아이콘 (red) + retry 버튼
```

## 회의 실행 화면 레이아웃
```
┌─────────────────────────────────┐
│ 회의 주제: [주제 텍스트]          │
│ ████████████████░░░░ 6/8 완료   │  ← MeetingProgress 바
├──────────────────────────────────┤
│ [지민 ✓] [현우 ✓] [소연 ◌]      │  ← AgentCard 그리드
│ [태준 ✓] [미래 ▶] [윤서 ◌]      │
│ [다은 ✗] [승호 ▶]               │
├──────────────────────────────────┤
│ 완료된 팀원 결과 실시간 표시       │
└─────────────────────────────────┘
```

## 앱 레이아웃
```
┌──────────┬──────────────────────┐
│  사이드바  │    메인 패널          │
│ (팀원 목록)│ (회의 설정 또는 실행)  │
│  200px   │      나머지           │
└──────────┴──────────────────────┘
```

## 공통 상태 UI 패턴
```
빈 상태:
  아이콘 + "아직 팀원이 없어요" + [팀원 추가하기] 버튼 (v2용 placeholder)

로딩 상태:
  버튼 내 스피너 (제출 중) / 스켈레톤 (목록 로딩)

에러 상태:
  인라인 에러 (폼) / 토스트 (작업 결과) / 에러 아이콘+메시지 (카드)
```

## 다크모드
- CSS 변수로 색상 정의 (`--color-primary` 등) — 하드코딩 금지
- shadcn/ui의 `dark:` 클래스 활용

## 회의 중 확인할 것
1. shadcn/ui에 이미 있는 컴포넌트를 쓸 수 있는가?
2. 5가지 카드 상태가 시각적으로 명확하게 구분되는가?
3. 1024×768에서 레이아웃이 깨지지 않는가?
4. 다크모드에서 색상 하드코딩된 부분이 없는가?

## 의견 형식
- **컴포넌트**: (shadcn/ui 재사용 / 신규 필요)
- **상태 표현**: (어떻게 시각화할 것인가)
- **레이아웃**: (1024×768 기준 배치)
- **공수**: (신규 컴포넌트 설계 필요 여부)
